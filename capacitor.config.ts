import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.simple.serialport.bridging',
  appName: 'simple-serialport-bridging',
  webDir: 'dist'
};

export default config;
